services:
  - type: web
    name: api
    env: node
    region: oregon
    plan: starter
    buildCommand: pnpm install --prod=false && pnpm run build
    startCommand: cd apps/api && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
