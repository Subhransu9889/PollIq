services:
  - type: web
    name: api
    env: node
    region: oregon
    plan: starter
    buildCommand: pnpm install --frozen-lockfile --prod=false && pnpm --filter @repo/api build
    startCommand: pnpm --filter @repo/api start
    envVars:
      - key: NODE_ENV
        value: production
